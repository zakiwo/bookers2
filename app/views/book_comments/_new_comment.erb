<div class="new-comments">
  <%= form_with(model:[book, comment], remote: true) do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, size: "60 x 5", placeholder:"コメントをここに" %>
  </div>
  <div class="form-group">
    <%= f.submit "送信" %>
  </div>
  <% end %>
</div>
</div>